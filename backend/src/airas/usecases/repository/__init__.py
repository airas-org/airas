"""`prepare_repository` and `set_github_actions_secrets` are the entry points
the MCP tools wrap; `nodes/` holds the operations `prepare_repository`
composes. `StateGraph` wrappers live in `workflows/repository`."""
