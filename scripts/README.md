# 実験手順


- bibfileの中身の作成は一度作成しておく、あとは使いまわせる
  - bibfileの作成(API)
  - bibfileは送られているか確認する


- 論文内容を生成する形に成形(手動)
  - https://github.com/auto-res2/kg-extraction-experiment
- 論文内容の生成(API)
- Latexファイルの内容を生成(API)
- リポジトリの作成(API)
- Latexファイルをpushする(API)
- Latexファイルのコンパイル(API)

