{
  "PostToolUse": [
    {
      "matcher": "Write|Edit|implement",
      "hooks": [
        {
          "type": "command",
          "command": "pre-commit run ruff --all-files && pre-commit run mypy --all-files && pre-commit run biome --all-files"
        }
      ]
    }
  ]
}
